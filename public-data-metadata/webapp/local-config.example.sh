#!/usr/bin/env bash
# Copy this file to local-config.sh (same folder) and fill in your real
# values below. local-config.sh is gitignored - it's never committed, so
# your credentials stay on your machine only.
#
#   cp local-config.example.sh local-config.sh
#
# Then just double-click start-server.command from now on.

export OPENSKY_CLIENT_ID=""
export OPENSKY_CLIENT_SECRET=""
export WSDOT_ACCESS_CODE=""
export NY511_API_KEY=""
